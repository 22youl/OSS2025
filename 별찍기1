#include <iostream>
using namespace std;
int N;
void A(int N)
{
	if (N == 0)
		return;
	A(N-1);
	for (int i = 0; i < N; i++)
		cout << "*";
	cout << "\n";
}
int main()
{
	cin >> N;
	A(N);
	return 0;
}
